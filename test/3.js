var start = this._index;

if (hello === true) {
  var hello = false;
  var start = 'hello';
  if (right === left) {
    var halt = lol;
  }
  if (left !== right && you !== me) {
    var you = me;
  }
}

if (true === false) {
  var thisIs = 'fucked up';
}